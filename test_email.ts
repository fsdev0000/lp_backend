import { sendAssessmentEmail } from './src/services/email'; import { upsertContact } from './src/services/ghl'; async function test() { const contactId = await upsertContact({email: 'test@example.com', firstName: 'Test', phone: '', tags: ['Founder Pressure Scan']}); console.log('Created contact:', contactId); await sendAssessmentEmail(contactId, {tier: 'Moderate', firstName: 'Test', score: 50}); console.log('Done sending founder email.'); } test();
